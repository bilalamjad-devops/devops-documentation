ma na ya lab likha ha kasi ha, :



Terms: 
inventory 

Playbook 

Module 

Modle: small task is called module 
- install nginx
- start docker 
start a server 
update create a file 


commands or set of commands to be executed on client side 

inventory:
remote servers ka bary my information 

aasi file jis ma say servers ki information rhti ha 

playbook:
to isntall web server on remote server 



in this lab, we create 1 master server and three host servers. we install ansible on master server...

sudo apt update

sudp apt install ansible -y 


cd .ssh 

vim ansible_key 

paste your key here 

esc:wq! 

pwd: /home/ubuntu/.ssh 


sudo ssh -i ~/.ssh/ansible_key ubuntu@publicip 


you have entered ffrom one server to another server. it is called secure shell. or ssh 


exit 

---

cat /etc/ansible/hosts
no file 

aap inventory kahin pa bi bna skty ho bus us ka path dena hota ha 
mkdir ansible 

cd ansible/ 
vim hosts 

[servers]
server1 ansible_host=publicip1 
server2 ansible_host=publicip2 
server3 ansible_host=publicip3 

[all:vars]
ansible_python_interpreter=/usr/bin/python3 

esc:wq 

ansible-inventory --list -y -i /home/ubuntu/ansible 

change permission:

chmod 700 ~/.ssh 
chmod 600 ~/.ssh/ansible_key


ansible all -m ping -i /home/ubuntu/ansible/hosts --private-key=~/.ssh/ansible_key

ansible all -a "free -h" -i /home/ubuntu/ansibe/hosts --private-key=~/.ssh/ansible_key

ansible all -a "uptime" -i /home/ubuntu/ansibe/hosts --private-key=~/.ssh/ansible_key

what do you think how much ansible mjy aa gya ha? and please write this notes into medium aritcle format
