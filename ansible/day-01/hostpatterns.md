### Host Patterns ⭐⭐⭐⭐☆

```
ansible all

ansible web

ansible web[0]
```

Concept:

Instead of targeting IP addresses directly, you target groups or subsets of hosts.

This makes commands more flexible and scalable.
